# Dev/test dependencies for CI — exact pins (no drift).
# Dependabot can bump these; keep them in sync with the local .venv.
# Exercise the complete shipped runtime in every clean test environment.
-r service/requirements-app.txt
pytest==9.1.1
pip-audit==2.10.1
openapi-spec-validator==0.9.0
ruff==0.16.3
# Validates emitted artifacts against the published Draft 2020-12
# schemas in service/scripts/schemas/ (tests/test_artifact_schemas.py).
# Was already present transitively via openapi-spec-validator; pinned
# explicitly because tests import it directly.
jsonschema==4.26.0

# HTTP test client (not needed by the shipped service).
httpx==0.28.1
# Differential-testing oracle for the hand-rolled RFC 3161 DER/CMS parser
# in tools/counselclear_verify_release_packet.py (tests only — the shipped
# verifier itself stays stdlib-only, per its own import-discipline tests).
# Same test-only-oracle pattern as cryptography in the Ed25519 cross-check.
asn1crypto==1.5.1
