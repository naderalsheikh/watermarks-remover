"""CounselClear engine package.

JSON Schema lives in ``engine/schemas/``. Processing code stays in
``service/scripts`` (this repo's deny-by-default gitignore only allowlists
``engine/schemas``).
"""
